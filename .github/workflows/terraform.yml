name: terraform setup

on:
  push:
    branches:
      - main

jobs:
  create-infra:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Terraform Setup
        uses: hashicorp/setup-terraform@v4

